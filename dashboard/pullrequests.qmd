---
format:
    dashboard:
        scrolling: true
        title: "Pull Requests"
---


# All Time

## Row

```{python}
import htmltools
import pandas as pd
import matplotlib.pyplot as plt
import numpy as np
import sys
import os
import datetime


project_root = os.path.abspath(os.path.join(os.getcwd(), '..'))

if project_root not in sys.path:
    sys.path.insert(0, project_root)

import src.chartkit as ck
from src.metrics_calculator import MetricsCalculator
from src.database import DatabaseManager

ck.apply_style()

calculator_30d = MetricsCalculator(db_path="../data/analytics.db", days=30)
calculator_60d = MetricsCalculator(db_path="../data/analytics.db", days=60)
calculator_90d = MetricsCalculator(db_path="../data/analytics.db", days=90)
calculator_all = MetricsCalculator(db_path="../data/analytics.db", days=None)

db = DatabaseManager("../data/analytics.db")

```

```{python}
def visualise_merge_time_distribution(calculator: MetricsCalculator) -> None:
    fig, ax = ck.question_figure("How long do PRs wait before merging?")
    merge_times_df = calculator.merge_time_distribution()
    merge_times = merge_times_df['merge_time_days'] if not merge_times_df.empty else pd.Series(dtype=float)
    bins = [0, 1, 3, 7, 14, 30, np.inf]
    labels = ['<1d', '1-3d', '3-7d', '7-14d', '14-30d', '>30d']
    binned = pd.cut(merge_times, bins=bins, labels=labels, right=False)
    counts = binned.value_counts().reindex(labels, fill_value=0)
    dist = pd.DataFrame({'bucket': labels, 'count': counts.values})
    if dist['count'].sum() == 0:
        ck.empty_state(ax, "No merged PRs in this period yet.")
        plt.tight_layout()
        plt.show()
        return
    ck.bars(ax, dist, x='bucket',
            series=[{'col': 'count', 'label': 'PRs'}],
            ylabel='Number of PRs')
    plt.tight_layout()
    plt.show()
```

```{python}
def visualise_pr_size_distribution(calculator: MetricsCalculator) -> None:
    fig, ax = ck.question_figure("How big are the changes we review?")
    pr_sizes_df = calculator.pr_size_distribution()
    pr_sizes = pr_sizes_df['size'] if not pr_sizes_df.empty else pd.Series(dtype=float)
    bins = [0, 50, 200, 500, 1000, np.inf]
    labels = ['<50', '50-200', '200-500', '500-1000', '>1000']
    binned = pd.cut(pr_sizes, bins=bins, labels=labels, right=False)
    counts = binned.value_counts().reindex(labels, fill_value=0)
    dist = pd.DataFrame({'bucket': labels, 'count': counts.values})
    if dist['count'].sum() == 0:
        ck.empty_state(ax, "No PRs in this period yet.")
        plt.tight_layout()
        plt.show()
        return
    ck.bars(ax, dist, x='bucket',
            series=[{'col': 'count', 'label': 'PRs'}],
            ylabel='Number of PRs')
    plt.tight_layout()
    plt.show()
```

```{python}
def visualise_merge_time_trend(calculator: MetricsCalculator) -> None:
    fig, ax = ck.question_figure("Are community PRs merging as fast as core-team PRs?")
    df = calculator.get_pr_merge_time_trend(freq='QE')
    if df.empty:
        ck.empty_state(ax, "No merged PRs to chart yet.")
        plt.tight_layout()
        plt.show()
        return
    df = df.copy()
    df['period'] = pd.to_datetime(df['period'])
    ck.timeseries(
        ax, df, x='period',
        series=[{'col': 'community', 'label': 'Community', 'n_col': 'community_n'},
                {'col': 'core', 'label': 'Core team', 'n_col': 'core_n'}],
        ylabel='Median days to merge', min_n=5)
    plt.tight_layout()
    plt.show()
```

```{python}
def visualise_first_response_trend(calculator: MetricsCalculator) -> None:
    fig, ax = ck.question_figure("How quickly does someone respond to a new PR?")
    df = calculator.get_pr_first_response_trend(freq='QE')
    if df.empty:
        ck.empty_state(ax, "No PR responses to chart yet.")
        plt.tight_layout()
        plt.show()
        return
    df = df.copy()
    df['period'] = pd.to_datetime(df['period'])
    ck.timeseries(ax, df, x='period',
                  series=[{'col': 'median_days', 'label': 'Median first response'}],
                  ylabel='Days to first response')
    plt.tight_layout()
    plt.show()
```

::: {.valuebox icon="arrow-down-up" color="vb-primary" title="Median Merge Time (days)"}

`{python} float("{:.2f}".format(calculator_all.get_median_pr_merge_time()))`

```{python}
htmltools.HTML(f"<span>days to merge a pull request that was opened during this time window")
```

:::

::: {.valuebox icon="person-check-fill" color="vb-primary" title="Time to First Response (days)"}

`{python} float("{:.2f}".format(calculator_all.get_med_time_to_first_response_prs()))`

```{python}
htmltools.HTML(f"<span>days to respond to a pull request that was opened during this time window")
```



:::

::: {.valuebox icon="clock-fill" color="vb-primary" title="Merge Rate"}

`{python} float("{:.2f}".format(calculator_all.get_pr_merge_rate()["merge_rate"]))`

```{python}
htmltools.HTML(f"<span>{calculator_all.get_pr_merge_rate()["total_merged"]} PRs merged out of {calculator_all.get_pr_merge_rate()["total_prs"]} total PRs.</span>")
```

:::

::: {.valuebox icon="file-diff" color="vb-primary" title="PR Backlog"}

`{python} calculator_all.backlog_trend_prs()`

```{python}
htmltools.HTML(f"<span>PRs open</span>")
```

:::

::: {.valuebox icon="people" color="vb-primary" title="Community Merge Time (days)"}

`{python} float("{:.2f}".format(calculator_all.get_median_pr_merge_time_by_group()["community"]))`

```{python}
htmltools.HTML(f"<span>vs {calculator_all.get_median_pr_merge_time_by_group()["core"]:.2f} days for core team</span>")
```

:::

## Row

::: {.card}

```{python}
visualise_merge_time_distribution(calculator=calculator_all)
```

:::

::: {.card}

```{python}
visualise_pr_size_distribution(calculator=calculator_all)
```

:::

## Row

::: {.card}

```{python}
visualise_merge_time_trend(calculator=calculator_all)
```

:::

::: {.card}

```{python}
visualise_first_response_trend(calculator=calculator_all)
```

:::

## Row

`{python} "Last Sync Time: " + datetime.datetime.fromisoformat(db.get_last_sync_time().replace('Z', '+00:00')).strftime('%b %d, %Y %I:%M %p') + " UTC"`

## Row

# Last 90 Days

## Row

::: {.valuebox icon="arrow-down-up" color="vb-primary" title="Median Merge Time (days)"}

`{python} float("{:.2f}".format(calculator_90d.get_median_pr_merge_time()))`

```{python}
htmltools.HTML(f"<span>days to merge a pull request that was opened during this time window")
```

:::

::: {.valuebox icon="person-check-fill" color="vb-primary" title="Time to First Response (days)"}

`{python} float("{:.2f}".format(calculator_90d.get_med_time_to_first_response_prs()))`

```{python}
htmltools.HTML(f"<span>days to respond to a pull request that was opened during this time window")
```



:::

::: {.valuebox icon="clock-fill" color="vb-primary" title="Merge Rate"}

`{python} float("{:.2f}".format(calculator_90d.get_pr_merge_rate()["merge_rate"]))`

```{python}
htmltools.HTML(f"<span>{calculator_90d.get_pr_merge_rate()["total_merged"]} PRs merged out of {calculator_90d.get_pr_merge_rate()["total_prs"]} total PRs.</span>")
```

:::

::: {.valuebox icon="file-diff" color="vb-primary" title="PR Backlog"}

`{python} calculator_90d.backlog_trend_prs()`

```{python}
htmltools.HTML(f"<span>PRs open</span>")
```

:::

::: {.valuebox icon="people" color="vb-primary" title="Community Merge Time (days)"}

`{python} float("{:.2f}".format(calculator_90d.get_median_pr_merge_time_by_group()["community"]))`

```{python}
htmltools.HTML(f"<span>vs {calculator_90d.get_median_pr_merge_time_by_group()["core"]:.2f} days for core team</span>")
```

:::

## Row

::: {.card}

```{python}
visualise_merge_time_distribution(calculator=calculator_90d)
```

:::

::: {.card}

```{python}
visualise_pr_size_distribution(calculator=calculator_90d)
```

:::


## Row

`{python} "Last Sync Time: " + datetime.datetime.fromisoformat(db.get_last_sync_time().replace('Z', '+00:00')).strftime('%b %d, %Y %I:%M %p') + " UTC"`

## Row


# Last 60 Days

## Row

::: {.valuebox icon="arrow-down-up" color="vb-primary" title="Median Merge Time (days)"}

`{python} float("{:.2f}".format(calculator_60d.get_median_pr_merge_time()))`

```{python}
htmltools.HTML(f"<span>days to merge a pull request that was opened during this time window")
```

:::

::: {.valuebox icon="person-check-fill" color="vb-primary" title="Time to First Response (days)"}

`{python} float("{:.2f}".format(calculator_60d.get_med_time_to_first_response_prs()))`

```{python}
htmltools.HTML(f"<span>days to respond to a pull request that was opened during this time window")
```


:::

::: {.valuebox icon="clock-fill" color="vb-primary" title="Merge Rate"}

`{python} float("{:.2f}".format(calculator_60d.get_pr_merge_rate()["merge_rate"]))`

```{python}
htmltools.HTML(f"<span>{calculator_60d.get_pr_merge_rate()["total_merged"]} PRs merged out of {calculator_60d.get_pr_merge_rate()["total_prs"]} total PRs.</span>")
```

:::

::: {.valuebox icon="file-diff" color="vb-primary" title="PR Backlog"}

`{python} calculator_60d.backlog_trend_prs()`

```{python}
htmltools.HTML(f"<span>PRs open</span>")
```

:::

::: {.valuebox icon="people" color="vb-primary" title="Community Merge Time (days)"}

`{python} float("{:.2f}".format(calculator_60d.get_median_pr_merge_time_by_group()["community"]))`

```{python}
htmltools.HTML(f"<span>vs {calculator_60d.get_median_pr_merge_time_by_group()["core"]:.2f} days for core team</span>")
```

:::


## Row

::: {.card}

```{python}
visualise_merge_time_distribution(calculator=calculator_60d)
```

:::

::: {.card}

```{python}
visualise_pr_size_distribution(calculator=calculator_60d)
```

:::


## Row

`{python} "Last Sync Time: " + datetime.datetime.fromisoformat(db.get_last_sync_time().replace('Z', '+00:00')).strftime('%b %d, %Y %I:%M %p') + " UTC"`

## Row

# Last 30 Days

## Row

::: {.valuebox icon="arrow-down-up" color="vb-primary" title="Median Merge Time (days)"}

`{python} float("{:.2f}".format(calculator_30d.get_median_pr_merge_time()))`

```{python}
htmltools.HTML(f"<span>days to merge a pull request that was opened during this time window")
```

:::

::: {.valuebox icon="person-check-fill" color="vb-primary" title="Time to First Response (days)"}

`{python} float("{:.2f}".format(calculator_30d.get_med_time_to_first_response_prs()))`

```{python}
htmltools.HTML(f"<span>days to respond to a pull request that was opened during this time window")
```



:::

::: {.valuebox icon="clock-fill" color="vb-primary" title="Merge Rate"}

`{python} float("{:.2f}".format(calculator_30d.get_pr_merge_rate()["merge_rate"]))`

```{python}
htmltools.HTML(f"<span>{calculator_30d.get_pr_merge_rate()["total_merged"]} PRs merged out of {calculator_30d.get_pr_merge_rate()["total_prs"]} total PRs.</span>")
```

:::

::: {.valuebox icon="file-diff" color="vb-primary" title="PR Backlog"}

`{python} calculator_30d.backlog_trend_prs()`

```{python}
htmltools.HTML(f"<span>PRs open</span>")
```

:::

::: {.valuebox icon="people" color="vb-primary" title="Community Merge Time (days)"}

`{python} float("{:.2f}".format(calculator_30d.get_median_pr_merge_time_by_group()["community"]))`

```{python}
htmltools.HTML(f"<span>vs {calculator_30d.get_median_pr_merge_time_by_group()["core"]:.2f} days for core team</span>")
```

:::

## Row

::: {.card}

```{python}
visualise_merge_time_distribution(calculator=calculator_30d)
```

:::

::: {.card}

```{python}
visualise_pr_size_distribution(calculator=calculator_30d)
```

:::

## Row

`{python} "Last Sync Time: " + datetime.datetime.fromisoformat(db.get_last_sync_time().replace('Z', '+00:00')).strftime('%b %d, %Y %I:%M %p') + " UTC"`

## Row
